package com.example.comingsoon.ui.theme

import androidx.compose.ui.unit.sp

val VioletTypography = Typography.copy(
    headlineLarge = Typography.headlineLarge.copy(
        fontFamily = RubikPuddles
    )
)

val DarkBlueTypography = Typography.copy(
    headlineLarge = Typography.headlineLarge.copy(
        fontFamily = Flavors
    )
)

val LightBlueTypography = Typography.copy(
    headlineLarge = Typography.headlineLarge.copy(
        fontFamily = Cinzel
    )
)

val TealTypography = Typography.copy(
    headlineLarge = Typography.headlineLarge.copy(
        fontFamily = RubikScribble
    )
)

val DarkGreenTypography = Typography.copy(
    headlineLarge = Typography.headlineLarge.copy(
        fontFamily = MontserratUnderline
    )
)

val LightGreenTypography = Typography.copy(
    headlineLarge = Typography.headlineLarge.copy(
        fontFamily = CraftyGirls
    )
)

val YellowTypography = Typography.copy(
    headlineLarge = Typography.headlineLarge.copy(
        fontFamily = EmilysCandy
    )
)

val OrangeTypography = Typography.copy(
    headlineLarge = Typography.headlineLarge.copy(
        fontFamily = SofadiOne
    )
)

val PinkTypography = Typography.copy(
    headlineLarge = Typography.headlineLarge.copy(
        fontFamily = LeagueScript,
        fontSize = 42.sp
    )
)